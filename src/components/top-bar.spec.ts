// Import { fixture } from "@open-wc/testing";

// Import { TopBar } from './top-bar'

describe('tob-bar', (): void => {
  it('is defined', (): void => {
    expect.assertions(0);
    /*
     * Const element = document.createElement('my-element');
     * Expect(element).toBeInstanceOf(TopBar)
     */
  });
})